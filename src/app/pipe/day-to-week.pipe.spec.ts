import { DayToWeekPipe } from './day-to-week.pipe';

describe('DayToWeekPipe', () => {
  it('create an instance', () => {
    const pipe = new DayToWeekPipe();
    expect(pipe).toBeTruthy();
  });
});
