import { MusicTimePipe } from './music-time.pipe';

describe('MusicTimePipe', () => {
  it('create an instance', () => {
    const pipe = new MusicTimePipe();
    expect(pipe).toBeTruthy();
  });
});
